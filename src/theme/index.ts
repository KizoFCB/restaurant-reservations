import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#6315da",
    },
    secondary: {
      main: "#fb2475",
    },
    grey: {
      500: "#ffffff",
    },
  },
});

export default theme;
